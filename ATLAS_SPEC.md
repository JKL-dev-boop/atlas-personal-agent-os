# ATLAS 产品与系统规范

> 文档角色：ATLAS 的长期产品真相（living spec）  
> 状态：有效  
> 版本：0.2.1
> 最近更新：2026-09-06  
> 所有者：仓库所有者  
> 维护方式：每次确认增量需求后，与实现同批更新

## 1. 产品定义

ATLAS 是面向个人长期使用的、模块化、可扩展的 Agent OS。它不是单一日报网站或 PDF 文件柜，而是把信息采集、内容理解、项目追踪、知识沉淀、演示发布、自动任务和 Agent/Sandbox 实验统一到一个可迁移的个人工作台中。

当前第一目标是满足两项真实需求：

1. 每日生成并推送中文《GitHub 日报｜AI · 应用 · Agent Sandbox》，同时在网站保存完整内容与历史。
2. 保存和公开预览 PPT 形式的 PDF，便于从其他设备访问和向他人展示。

平台必须允许后续加入新模块，而不要求重写已有内容模型或导航体系。

## 2. 用户与使用场景

### 2.1 主要用户

- 所有者：管理内容、增量需求、自动任务、公开范围和开发方向。
- 公开访客：只访问被明确发布的日报、项目资料和演示文件。
- AI Agent：在授权范围内采集、整理、开发、验证、发布和维护文档。

### 2.2 核心场景

- 09:00 接收当天最值得看的 5 个 GitHub 项目摘要。
- 在网站查看当天完整 10 项日报并浏览历史、分类和标签。
- 追踪重复项目的新 Release、热度变化和再次推荐原因。
- 上传 PDF，生成稳定的浏览与分享入口，保留版本信息。
- 从任意新会话读取本规范，并基于用户增量需求继续设计和开发。

## 3. 产品原则

1. 仓库是可迁移的内容与规范源，网站是其中一种呈现方式。
2. 采集、生成、验证、发布、通知彼此解耦，单点失败不应伪造整体成功。
3. 公开与私密默认分层；没有明确公开意图的个人数据不得进入公开目录。
4. 所有自动结论可追溯到来源、时间和处理状态。
5. 功能通过模块注册和统一内容信封扩展，避免页面级孤岛。
6. 优先交付可用的纵向切片，再逐步增加复杂能力。
7. AI 生成内容必须可评测、可回退、可人工修正。

## 4. 功能模块

| 模块 | 状态 | 当前职责 | 下一阶段 |
| --- | --- | --- | --- |
| 总览 | 已启用 | 跨模块入口和状态摘要 | 个性化待办与异常聚合 |
| 情报日报 | 已启用/待真实数据 | 10 项日报、5 项精选、分类与来源 | 稳定采集器、评分和历史对比 |
| 项目雷达 | 已启用/待快照 | 追踪 Star、Release、重复变化 | 订阅列表、趋势图与告警 |
| 演示资料 | 已启用/待上传 | PDF 元数据、版本、预览和分享 | 上传流程、封面生成、权限层 |
| 自动任务 | 已启用 | 展示调度与刷新状态 | GitHub Actions 后台流水线与重试 |
| 知识库 | 规划中 | 收藏、笔记与文档 | 搜索、关联与引用 |
| Sandbox 实验室 | 规划中 | 隔离执行实验的入口 | 生命周期、调度、指标与对比 |

新增模块必须在 `lib/module-registry.ts` 注册，并复用统一内容元数据；只有出现明确跨模块价值时才扩展核心模型。

## 5. GitHub 日报规范

### 5.1 采集范围

- 主要来源：GitHub Trending 日榜。
- 持续热度参考：GitHub Trending 周榜。
- 补充来源：相关领域重要新项目和 Release，必须明确标注为补充来源。
- 分区：AI / Agent、应用项目、K8s / 容器 / Sandbox。

### 5.2 内容质量

- 每日精选 10 个项目，其中标记最值得看的 5 个。
- 优先检查 README、官方文档、依赖配置和 Release，不能只依据仓库语言猜测。
- 每项包含：功能概要、技术栈、热度依据、与 Agent Sandbox 的关联、成熟度与限制、阅读建议、来源。
- 尽量避免连续重复；重复项目必须说明本次新增变化。
- Star 日增量只使用可靠页面数据或 ATLAS 连续快照，无法确认时写 `null`，不得估算。

### 5.3 发布和失败语义

- 内容日期、来源日期、最后成功时间和本次运行状态必须分别保存。
- 只有完整通过结构与质量校验的日报才能成为网站“今日内容”。
- 采集失败时保留最后一期成功内容，同时展示本次失败和失败时间。
- 09:00 通知优先交付 5 项摘要；网站或 GitHub 更新失败不能吞掉通知，且必须说明未完成部分。

## 6. 演示资料规范

- 公开文件放在 `public/presentations/`，并使用稳定、无敏感信息的文件名。
- 每个文件有独立内容记录：标题、摘要、版本、公开性、页数、文件路径和更新时间。
- 默认保存原 PDF；有源 PPTX 时，只有用户明确允许公开才可上传。
- 替换文件时递增版本并保留变更说明；不得静默覆盖已经对外分享的重要版本。
- 公开页面需支持桌面和移动端浏览，并提供直接打开/下载入口。

## 7. 目标运行流程

```text
定时或手动触发
  → 获取候选与昨日快照
  → 获取 README / 文档 / Release
  → 结构化提取与分类
  → 规则校验与 AI 评审
  → 生成 10 项日报和 5 项摘要
  → 原子写入内容与运行状态
  → 构建与发布网站
  → 提交 GitHub
  → 在当前任务通知结果
```

目标形态分为两层：

- 后台确定性层：GitHub Actions 负责计划触发、快照、校验、构建、归档和可重复执行，电脑关机时仍可运行。
- Agent 判断层：Codex 负责深度阅读、相关性判断、内容解释、异常修复和增量开发。

当前现实：Codex 的 09:00 会话内定时任务已启用；独立采集器、连续快照存储和完整 GitHub Actions 日报流水线尚未完成。因此在完成后台流水线前，网站更新仍是 Agent 运行时的尽力执行。

## 8. 数据与架构

### 8.1 当前技术基线

- Web：React 19、TypeScript、vinext、Vite、Cloudflare Workers/Sites。
- 包管理：pnpm；保留现有锁文件。
- 领域模型：`lib/content-model.ts`。
- 模块注册：`lib/module-registry.ts`。
- 内容：仓库内版本化 JSON/静态文件；未来需要高频查询或私密数据时再引入 D1/R2。

### 8.2 统一内容信封

所有内容记录至少拥有：稳定 ID、模块、标题、slug、摘要、状态、标签、创建时间、更新时间和模块载荷。时间使用 ISO 8601，调度和展示默认时区为 `Asia/Shanghai`。

### 8.3 数据边界

- 可以公开：已审核日报、明确公开的 PDF、公共仓库元数据和公开来源链接。
- 不得提交：访问令牌、私钥、Cookies、个人账号数据、未公开文档、原始私密提示或本机凭据。
- 私密能力启用前必须先设计身份、授权、存储和导出/删除机制。

## 9. 非功能要求

- 可用性：核心阅读路径适配移动端；公开链接稳定。
- 可访问性：语义化结构、键盘可达、可见焦点、合理对比度。
- 性能：首页优先展示可操作内容，不让装饰性资源阻塞核心信息。
- 可靠性：自动任务幂等；重复执行同一日期不会产生冲突记录。
- 可观测性：每次运行保存开始、结束、状态、错误摘要和输出记录 ID。
- 安全性：最小权限、密钥隔离、外部内容不可信、高风险写操作可审查。
- 可维护性：模块边界明确，规范、实现和测试同批更新。

## 10. 验收标准

### 日报纵向切片

- 可生成符合模型的 10 项日报，其中恰有 5 项精选。
- 所有项目有来源；未知 Star 增量为 `null`。
- 失败运行不替换最后成功日报。
- 网站可浏览当天内容和至少一个历史日期。
- 09:00 通知明确区分全部成功、部分成功和失败。

### 演示资料纵向切片

- 一个公开 PDF 可通过稳定网址打开并下载。
- 页面展示标题、摘要、版本和更新时间。
- 更新版本后旧链接行为有明确策略且不会误导访客。

### 跨会话连续开发

- 新会话只需读取 `AGENTS.md` 即可定位本规范和开发规范。
- 新的已确认需求会反映在本规范的相应章节和变更记录中。
- 实现与规范冲突时，交付说明必须指出并解决冲突。

## 11. 路线图

### P0：可靠基础

- 固化 SPEC 和 AI 开发规范。（已完成）
- 建立 CI 质量门槛。（已完成：规范入口、lint、build）
- 完成日报采集、快照、校验、构建和发布的 GitHub Actions 流水线。
- 验证首个真实 09:00 运行闭环。

### P1：真实内容

- 接入首份真实日报和历史归档。
- 完成 PDF 上传/登记/预览纵向切片。
- 增加项目订阅和重复项目变化对比。

### P2：个人操作系统

- 知识库、搜索、私密身份层和个人偏好。
- Sandbox 实验记录、资源指标和比较视图。
- 可插拔通知渠道、导出与备份。

## 12. 已知决策与待确认项

已确认：

- 产品定位是个人使用的“大而全但模块化”的 Agent OS。
- GitHub 日报和 PDF 展示是首批能力，不是最终边界。
- 日报每天北京时间 09:00 推送。
- GitHub 仓库与公开网站用于跨设备访问和展示。

待后续按实际需求确认：

- 私密内容的身份方案和是否需要多用户协作。
- PDF 旧版本保留策略及单文件大小上限。
- 日报 AI 生成所使用的模型、成本上限和云端密钥方案。
- 09:00 是开始执行时间还是保证送达时间；目标后台流水线建议提前运行并在 09:00 前完成。

## 13. 变更记录

- 2026-09-06 / 0.2.1：接入首份真实日报；项目快照增加 Trending 周期/增量、热度说明与本次变化字段，建立后续跨日对比基线。
- 2026-09-06 / 0.2.0：建立跨会话 living spec；明确产品定位、首批能力、目标自动化架构、公开边界、验收标准和路线图；加入 GitHub Actions 基础质量门禁。
